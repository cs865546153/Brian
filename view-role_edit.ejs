<%- include header %>
<div class="container col-lg-12 col-md-12 col-sm-12 col-xs-12">
    <div class="row">
        <%- include left %>
        <div class="col-lg-10 col-md-10 col-sm-9 col-xs-9">
            <ol class="breadcrumb">
                <li><a href="/home">首页</a></li>
                <li><a href="#">系统管理</a></li>
                <li class="active">信息管理</li>
            </ol>
            <div class="row">
                <button type="button" class="btn btn-success" data-toggle="modal" data-target="#addRole">
                    <span class="glyphicon glyphicon-plus"></span> 添加角色
                </button>
            </div>
            <form class="form-horizontal col-lg-9" role="form" action="/updateRole" method="post">
                <input type="hidden" name="role_id" value="<%= role.role_id %>"/>
                <div class="form-group">
                    <label for="inputPassword" class="col-sm-2 control-label">角色名称</label>
                    <div class="col-sm-10">
                        <input name="role_name" type="text" class="form-control" placeholder="角色名称" value="<%= role.role_name %>">
                    </div>
                </div>
                <div class="form-group">
                    <label for="inputPassword" class="col-sm-2 control-label"><input type="checkbox" id="checkAll"/>
                        权限</label>
                    <div class="col-sm-10">
                        <label class="checkbox-inline">
                            <input type="checkbox" <% if(/user/.test(role.role_auths)){ %>checked<%}%> name="role_auths" value="user"> 用户管理
                        </label>
                        <label class="checkbox-inline">
                            <input type="checkbox" <% if(/role/.test(role.role_auths)){ %>checked<%}%> name="role_auths" value="role"> 角色管理
                        </label>
                        <label class="checkbox-inline">
                            <input type="checkbox" <% if(/sms/.test(role.role_auths)){ %>checked<%}%> name="role_auths" value="sms"> 信息管理
                        </label>
                    </div>
                </div>
                <div class="form-group text-center">
                    <button type="submit" class="btn btn-primary">提交</button>
                    <button type="button" class="btn btn-default" onClick="javascript :history.go(-1);">取消</button>
                </div>
            </form>
        </div>
    </div>
</div>
<%- include footer %>
<script>
    $(function () {
        $("#checkAll").click(function () {
            $("input[name='auths']:checkbox").prop("checked", this.checked);
        });
    });
</script>
